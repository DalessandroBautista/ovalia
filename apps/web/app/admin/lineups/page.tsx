import { LineupEditor } from '../../../src/features/admin/lineup-editor';

export const metadata = {
  title: 'Cargar formaciones — Ovalia',
  robots: { index: false },
};

export default LineupEditor;
