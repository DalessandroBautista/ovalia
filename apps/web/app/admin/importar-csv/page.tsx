import { CsvImporter } from '../../../src/features/admin/csv-importer';

export const metadata = {
  title: 'Importar partidos por CSV — Ovalia',
  robots: { index: false },
};

export default CsvImporter;
